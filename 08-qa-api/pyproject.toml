[project]
name = "qa-api"
version = "0.1.0"
description = "FastAPI question answering service over retrieval and citation validation"
readme = "README.md"
authors = [
    { name = "dima55279", email = "dima55279@yandex.ru" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "pydantic-settings>=2,<3",
    "structlog>=24,<26",
    "uvicorn[standard]>=0.34,<1",
]

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.27,<1",
    "pytest>=8,<9",
    "pytest-asyncio>=0.23,<2",
    "ruff>=0.6,<1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
markers = [
  "integration: проверка с настоящей внешней библиотекой",
  "llm: проверка с настроенной языковой моделью",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
